[project]
name = "jsc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "lark>=1.3.1",
]

[dependency-groups]
dev = [
    "hypothesis>=6.151.11",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
]
